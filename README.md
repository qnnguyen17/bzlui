# `bzlui`, a Bazel Workspace Explorer

Pretty terminal UI for exploring Bazel workspaces and building/running/testing rules.

## Installation

```
cargo install --path .
```

## Usage

```
bzlui /path/to/workspace/root
```
